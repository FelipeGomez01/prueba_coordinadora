name: prueba_coordinadora
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  dio: ^4.0.6
  flutter_bloc: ^8.1.1
  flutter_secure_storage: ^7.0.1
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_slidable: ^2.0.0
  connectivity_plus: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  generate: true

  uses-material-design: true

  assets:
    - assets/images/